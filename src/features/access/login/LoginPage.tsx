import { PhasePlaceholder } from '../../../shared/components/PhasePlaceholder'
export function LoginPage() {
  return <PhasePlaceholder title="Iniciar sesión" description="Acceso común con validaciones y respuestas simuladas, sin sesión real." />
}
