import { PhasePlaceholder } from '../../../shared/components/PhasePlaceholder'
export function RecoverPasswordPage() {
  return <PhasePlaceholder title="Recuperar contraseña" description="Solicitud de recuperación de acceso con validación de correo y respuesta simulada." />
}
